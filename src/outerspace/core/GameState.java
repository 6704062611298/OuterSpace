package outerspace.core;

/**
 * High-level state of the game. The prototype only needs two states;
 * more (MENU, PAUSED, SHOP, ...) can be added later.
 */
public enum GameState {
    PLAYING,
    GAME_OVER
}
